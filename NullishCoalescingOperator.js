// Nullish Coalescing Operator

// ?? returns the first argument if it’s not null/undefined. Otherwise, the second one.

let user1;

console.log(user1 ?? 'Anonymous'); // Here user not defined, so it returns second one

let user2 = 'Prasanna';

console.log(user2 ?? 'Prasanna');
